import React from 'react';
import '../style/LoginLink.css';

const loginLink = () => {
  return (
    <div className="signup-link">
      Already have an account? <a href="/login">Log In</a>
    </div>
  );
};

export default loginLink;
