import React from 'react';
import '../style/SignUpLink.css';

const SignUpLink = () => {
  return (
    <div className="signup-link">
      Don’t have an account? <a href="/signup">Sign Up</a>
    </div>
  );
};

export default SignUpLink;
